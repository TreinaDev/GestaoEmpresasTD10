<h3>Empresas</h3>


<% if @companies.any? %>
  <ul>
  <% @companies.each do |company| %>
    <li>
      <%= link_to company.brand_name, company_path(company) %>
    </li>
  <% end %>
  </ul>
<% else %>
  <h3>Não há empresas cadastradas</h3>
<% end %>
